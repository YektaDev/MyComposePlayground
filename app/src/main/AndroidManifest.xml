<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="dev.yekta.mycomposeplayground">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.MyComposePlayground">
        <activity
                android:name=".Try2"
                android:exported="true"
                android:label="@string/title_activity_try2"
                android:theme="@style/Theme.MyComposePlayground.NoActionBar" />
        <activity
                android:name=".Try1"
                android:exported="true"
                android:label="@string/title_activity_try1"
                android:theme="@style/Theme.MyComposePlayground.NoActionBar" />
        <activity
                android:name=".MainActivity"
                android:exported="true"
                android:label="@string/app_name"
                android:theme="@style/Theme.MyComposePlayground.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>